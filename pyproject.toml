[project]
name = "language-pal"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "textual>=8.2.8",
    "textual-dev>=1.8.0",
]

[tool.pyright]
venvPath = "."
venv = ".venv"
